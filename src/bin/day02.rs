use aoc2022::{check_or_get_input, read_lines};

fn main () {
    let filepath = check_or_get_input(2).expect("Could not get input!");
    // let score = common(&filepath);
}

// fn common(filepath: &str) {
//     let lines: Vec<Vec<&str>> = read_lines(filepath).unwrap().map(|l| l.unwrap().as_str().split(' ').collect()).collect();


// }